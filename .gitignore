# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.traineddata

# Real slip photos used for local OCR testing - contain the account holder's
# name and partial account numbers, not meant to be pushed anywhere.
/Slip-Test/

# Database backups/exports - contain real user data (emails, balances). Local
# safety net only, never pushed.
/backups/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*

# Regenerable via `npx skills experimental_install` from skills-lock.json.
# .claude/skills symlinks use absolute paths, so they don't survive a clone.
.claude/skills
.agents/skills
